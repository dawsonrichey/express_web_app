doctype html
html(lang="en")
  head
    title #{title}
  body
    header
        h1 #{title}
    section#content
        ul
            each color in colors
                li #{color}
    footer
        p sdf